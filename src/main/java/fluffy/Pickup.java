package fluffy;

public class Pickup extends Sprite {
    public Pickup(GamePanel panel, Prefab prefab, float x, float y, float scale, float each_image_duration,
            float speed) {
        super(panel, prefab, x, y, scale, each_image_duration, speed);
    }
}
